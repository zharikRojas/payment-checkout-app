# Private SDD / statement (do not publish)
.cursor/sdd/

# Env / secrets
.env
.env.*
!.env.example

# Dependencies / build
node_modules/
dist/
build/
coverage/
*.log
.DS_Store

# OS / IDE noise
.idea/

# Local agent notes (do not publish)
AGENTS.md

# Vite / Nest
.vite/
*.tsbuildinfo

# Accidental tsc emit next to Prisma seed.ts (source of truth is seed.ts)
apps/api/prisma/seed.js
apps/api/prisma/seed.d.ts
apps/api/prisma/seed.js.map
